<div class="container px-5 px-sm-0">
	<div class="row">
		<div class="col-md-3">
		<!--<　% if @book.errors %>-->
		<!--<ul>-->
		<!--<　% @book.errors.full_messages.each do |message| %>-->
		<!--<li><　%= message %></li>-->
		<!--<　% end %>-->
		<!--</ul>-->
		<!--<　% end %>-->
			<%= render 'books/list', book: @book, user: @user %>
		</div>

		<div class="col-md-8 offset-md-1">
			<h2>Books</h2>
			<table class="table table-striped">
				<thead>
					<tr align="left">
						<th></th>
						<th>Title</th>
						<th>Opinion</th>
						<th colspan="3"></th>
					</tr>
				</thead>
				<tbody>
					<% @books.each do |book| %>
						<tr>
							<td>
								<div>
									<%= link_to user_path(book.user_id) do %>
									<%= attachment_image_tag book.user, :profile_image, :fill, 90, 100,fallback: "no_image.jpg", size:'40x40'  %>
									<% end %>
								</div>
							</td>
							<td><%= link_to book.title, book_path(book.id) %></td>
							<td><%= book.body %></td>

						    <!--いいね実装-->
	          		<!--< % if @book.favorite_by?(current_user) %>-->
	          		<!--	<td>-->
	          		<!-- 	< %= link_to book_favorites_path(book), method: :delete do %>-->
	          		<!-- 		♥< %= book.favorite.count %>-->
	          		<!-- 	< % end %>-->
	          		<!--	</td>-->
	          		<!--< % else %>-->
	          		<!--	<td>-->
	          		<!--	< %= link_to book_favorites_path(book), method: :post do %>-->
	          		<!--	　♡< %= book.favorite.count %>-->
	          		<!--	< % end %>-->
	          		<!--	</td>-->
	          		<!--< % end %>-->

						</tr>
					<% end %>
					</tbody>
			</table>
		</div>
	</div>
</div>
